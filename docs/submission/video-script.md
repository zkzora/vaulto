# Vaulto — demo video script (≤ 2:00)

Format: screen recording of the app + voice-over. Scene timings add up to 1:55. Keep the cursor slow; every label on screen is honest ("Simulated treasury", "Simulated on BNB mainnet", "Live mainnet · BNB", "DEFER · waiting NAV refresh").

Before recording: reset the demo state (Settings → Reset demo state), open `/evidence` in a second tab, and have the BNB wallet with ≥ 150 USDC connected in a third tab (Live scene).

| Time | On screen | Voice-over |
|---|---|---|
| 0:00–0:12 | Landing page, then click **Launch Vaulto** → **Continue with demo treasury**. Topbar shows "Simulated treasury" and "Simulated on BNB + Avalanche mainnet". | "Vaulto is an AI treasury desk for IXS RWA vaults. It runs on OpenServ: SERV reasoning decides, deterministic policy guardrails enforce the hard limits. Everything you'll see runs against the real IXS vaults on BNB Chain and Avalanche mainnet." |
| 0:12–0:25 | Home: total treasury $1.98M, idle $1.63M (BTC + USDC), Risk preference Balanced. Click **Run analysis**; the progress list shows the six agents. | "The simulated Acme DAO treasury holds 12.4 BTC and 595,000 USDC idle for 23 days. One click runs the pipeline: Treasury Scanner, Opportunity Finder, Risk Guardian pre-flight, Allocation Planner, SERV reasoning, Monitoring." |
| 0:25–0:50 | Strategy page → **SERV verdicts per vault**. Expand each row in this order: BTC Real Yield (REJECT), Avalanche open vault (DEFER, limit 0, NAV 10 days old), BNB licensed (REJECT, not whitelisted), BNB open (ALLOCATE 225,000 USDC). Show the pre-flight rows under the Avalanche vault (deposit limit, NAV freshness, cutoff). | "Every candidate gets a verdict with its reason. BTC Real Yield is rejected: announced by IXS, not deployed. The Avalanche vault is deferred: deposit limit zero and a NAV that is ten days old, so per IXS it is temporarily paused waiting for a NAV refresh. The licensed vaults are rejected: this wallet is not whitelisted. The open BNB vault passes every pre-flight check, so SERV allocates 225,000 USDC within the Planner's cap, keeping 71% liquid above the 30% floor." |
| 0:50–1:05 | Scroll to **Allocation memo**: Treasury condition, Policy, Proposed allocation, Deferred, Rejected, Risks, Execution. Then open **SERV reasoning · input and output** and expand "Decision output (verdicts JSON)". | "SERV also writes the investment-committee memo: the policy floor and cap, what is proposed, what is deferred and rejected and why, and the risks: RWA credit, the daily redemption cycle, the 0.5% redemption fee, NAV drift between updates. The exact facts we handed to SERV and the raw JSON it returned are one click away, and on the public evidence page." |
| 1:05–1:22 | Click **Approve & execute (Simulate)**. Modal: "Simulated on BNB mainnet", two steps (approve exact, deposit), eth_call with state override, expected 206,215 ixv1 at 1.0911 per share. Click **Approve & execute (Simulate)**; steps turn green; toast "simulation passed". | "Approve runs the exact approve and deposit calldata built by the IXS MCP through eth_call on BNB mainnet, with a state override for the balance and allowance. Expected shares, gas, price per share. No transaction is sent and any judge can run this from an empty wallet." |
| 1:22–1:45 | Switch to the tab with the real wallet (≥ 150 USDC on BNB). Topbar shows "Live mainnet · BNB". Run analysis → SERV allocates ~100 USDC (cap 150 USDC per transaction shown). **Approve & execute (Live)** → sign approve (exact amount) and deposit in the wallet → steps confirm → Activity shows the BscScan hashes; `/evidence` shows "Live mainnet" entries with block, gas and the ixv1 shares now held. | "With at least 100 USDC on BNB Chain, the same flow goes live: pre-flight, simulation from the real wallet state, memo, then the wallet signs an exact-amount approval and the deposit into the IX High Yield Bond vault. The hashes, the block and the shares received land on the evidence page automatically." |
| 1:45–1:55 | `/evidence`: vault table (deposit limits, NAV timestamps, last NAV change tx verified), next cutoff 17:00 SGT, watcher "waiting NAV refresh", fork runs, Export JSON. | "Every number has a source and a block: IXS MCP calls, subgraph NAV events, on-chain reads, the mainnet-fork runs. Vaulto: SERV decides, guardrails enforce, the evidence is public." |

Fallbacks:
- If the Live wallet is not funded, cut the Live scene to the Anvil fork run (`KEEP=1 npm run fork:demo`, topbar "Mainnet fork (BNB)"): same flow, label "Mainnet fork", and mention that the async vaults would show "Request submitted — pending operator settlement".
- If the Avalanche vault has reopened by recording day (limit > 0), the verdict becomes ALLOCATE or a split allocation; read the reason SERV gives instead of the deferral line.
